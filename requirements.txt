# Python dependencies for scripts/*.py (figure/video/data-export helpers
# that run against the C++ binary's CSV outputs). The C++ simulation itself
# has no Python dependency; this file only covers scripts/.
Pillow>=10.0
numpy>=1.24
imageio>=2.31
# render_gazebo_validation_video.py also needs an imageio video backend to
# actually write the .mp4 (imageio itself only provides the writer API):
imageio-ffmpeg>=0.4
